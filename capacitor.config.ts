import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Valhalla: Idle RPG',
  webDir: 'www'
};

export default config;
